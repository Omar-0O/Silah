# دليل تصميم واجهة وتجربة المستخدم لـ تطبيق "صلة الرحم" (Sila App)
## وثيقة المعايير الفنية والجمالية لـ Jetpack Compose 

تم إعداد هذا الدليل ليكون مرجعاً هندسياً وفنياً صارماً للمطورين ومصممي الواجهات، بهدف تقديم تجربة مستخدم ناعمة (Soft UI) ذات طابع عالمي، دافئة ومهدئة نفسياً، مع الابتعاد الكامل عن قوالب الذكاء الاصطناعي الجاهزة أو التصاميم النمطية المستهلكة.

---

## 1. فلسفة الأيقونة المبتكرة ومفهوم الاتصال المجرد (App Icon Architecture)
تم استبدال فكرة "القلب" المستهلكة أو الرموز التقليدية بمفهوم **"عقدة الوصل" (The Infinity Kinship Knot)**:
- **المفهوم:** خطوط هندسية انسيابية ومتداخلة تمثل شبكة العلاقات الأسرية، حيث تلتقي الفروع والروابط في عقد ناعمة تعبر عن الوالدين والأقارب.
- **التصميم الهندسي في Vector Drawable:**
  - مركز التصميم يحتوي على حلقتين ذهبية وزيتونية متداخلتين لتمثيل الاستمرارية والالتزام بالاتصال.
  - ثلاث نقاط محورية (Nodes) متألقة ومغلفة بألوان هادئة تمثل الجذور (الآباء) والاتصالات الحالية والمستقبلية.
  - مسارات دائرية ناعمة ومنحنيات Bezier تربط هذه النقاط لتعزيز فكرة "الوصول السلس والامتداد العائلي".

---

## 2. الهوية البصرية والتناسق الهندسي (Visual Tokens & Design System)

### أ. لوحة الألوان السيادية (Color System - Sila Palette)
تعتمد لوحة الألوان على درجات مستوحاة من الطبيعة والهدوء النفسي (Olive & Warm Sand):

```kotlin
// الرموز البرمجية للألوان الأساسية في ملف Color.kt
val PrimaryGreen = Color(0xFF1E5A35)       // زيتوني داكن فخم (يعبر عن النماء والأصالة)
val PrimaryGreenLight = Color(0xFF94DAB2)  // أخضر مهدئ وناعم للمظهر الداكن
val SecondaryGreyGreen = Color(0xFF4C6B56) // زيتوني رمادي متوسط للمؤشرات والنصوص الثانوية
val SoftGold = Color(0xFFE9CE79)           // ذهبي دافئ (لأوقات التواصل والتذكيرات الهامة)
val BackgroundSand = Color(0xFFFAF9F5)     // لون الرمل الدافئ (خلفية التطبيق الأساسية المريحة للعين)
val DeepCharcoal = Color(0xFF141816)       // فحمي عميق بلمسة زيتونية (خلفية المظهر الداكن)
val SurfaceDark = Color(0xFF1B221E)        // سطح داكن مريح ومقاوم للوهج
```

### ب. خطوط وتوزيع النصوص (Typography & Hierarchy)
نعتمد خط **Cairo** أو **Neo Sans** لجماليات انحناءاته وسلاسته في القراءة لكبار السن والشباب:
- **العناوين الرئيسية الكبيرة (Display H1):** `fontSize = 24.sp` | `fontWeight = FontWeight.Black` | `lineHeight = 32.sp` (يُستخدم لرسائل الترحيب وتأكيد النجاح).
- **العناوين الفرعية للبطاقات (Header H2):** `fontSize = 16.sp` | `fontWeight = FontWeight.Bold` | `lineHeight = 22.sp` (لعناوين الأقارب والتذكيرات).
- **نصوص القراءة والتفاصيل (Body Text):** `fontSize = 13.sp` | `fontWeight = FontWeight.Medium` | `lineHeight = 18.sp` (لملاحظات التواصل والتواريخ).
- **الملصقات والإحصائيات (Labels):** `fontSize = 11.sp` | `fontWeight = FontWeight.Normal` (للحالات والتواريخ الدقيقة).

### ج. الحواف والانحناءات والأعماق (Curves & Micro-shadows)
لمنع حدة التصميم وخلق واجهة ناعمة مريحة للمس:
- **زوايا البطاقات والديالوجات الرئيسية (Cards/Dialogs):** `RoundedCornerShape(24.dp)` أو `RoundedCornerShape(26.dp)`.
- **زوايا المدخلات النصية (TextFields):** `RoundedCornerShape(16.dp)`.
- **زوايا الأزرار التفاعلية (Buttons):** `RoundedCornerShape(14.dp)`.
- **الظلال الناعمة والعميقة (Soft Depth):**
  - تجنب استخدام الـ `elevation` الافتراضي الجاف.
  - اعتمد تراكيب الظلال باستخدام تلوين ألفا (Alpha) لـ `Shadow` أو `border` بلون ذهبي/زيتوني شفاف ليوفر عمقاً أنيقاً (مثال: `BorderStroke(1.dp, Color(0xFFE9CE79).copy(alpha = 0.3f))`).

---

## 3. الحركة والانتقالات الرشيقة (Animations & Micro-interactions)

- **انتقال التبويبات والشاشات:** استخدام `AnimatedContent` أو `AnimatedVisibility` مع تأثيرات التلاشي التدريجي والامتداد الطبيعي (`fadeIn + expandVertically`).
- **مؤثرات اللمس (Ripple Effect):** تهيئة لون وميض الضغط ليتناسق مع السطوع بدلاً من الرمادي الافتراضي:
  ```kotlin
  // وميض ذهبي خفيف عند الضغط على الأزرار الزيتونية
  Modifier.clickable(
      interactionSource = remember { MutableInteractionSource() },
      indication = rememberRipple(color = SoftGold.copy(alpha = 0.2f))
  ) { /* تفاعل */ }
  ```
- **حركة مؤشرات الإنجاز:** عند تحديث الالتزام الأسبوعي بصلة الرحم، تتحرك الدوائر التفاعلية باستخدام حركة متباطئة مرنة `spring(dampingRatio = Spring.DampingRatioMediumBouncy)`.

---

## 4. بنية وتدفق الشاشات الأساسية (Wireframing & Screens Architecture)

### أ. الشاشة الرئيسية (Dashboard):
1. **الترويسة (Header Panel):** ترحيب دافئ ومميز مبني على المساحات الواسعة، يعلوه مؤشر دائري فني (Progress Arc) ناعم يبين "معدل الود العائلي" ونسبة الالتزام الأسبوعية بالتواصل.
2. **شريط البحث والتصفية (Search & Filter Tabs):** فلترة ذكية مدمجة لفرز الأقارب (الوالدين، الإخوة، الأرحام، الأصدقاء) عبر حبات خيار ناعمة مستديرة الحواف.
3. **بطاقات الأقارب (Relative Cards):**
   - تصميم أفقي وعمودي انسيابي.
   - البطاقة غنية بالمساحات الفارغة المتزنة، تظهر الاسم، صلة القرابة، وتاريخ آخر تواصل.
   - رمز التنبيه يظهر باللون الذهبي الدافئ فقط عندما يحين موعد التواصل، ليتفادى المستخدم الشعور بالذنب أو الضغط النفسي بل يدفعه الود للمبادرة.

### ب. شاشة الملف الشخصي للقريب (Relative Profile):
- **الهوية البصرية:** ترويسة ناعمة بملف دائري ومكتوب تحته صلة القرابة ومعدل الانتظام.
- **الجدول الزمني الذكي (Timeline Layout):**
  - خط متقطع دقيق (Dotted line) بلون زيتوني شفاف يربط نقاط التواصل السابقة.
  - كل نقطة تواصل تظهر بنبضة خفيفة (Pulse Circle) تحتوي على أيقونة طريقة التواصل (مكالمة، زيارة، هدية) مع تعليق جانبي ناعم وخط مريح.

### ج. شاشة إضافة قريب وتخصيص التذكيرات:
- **التصميم الخالي من التكدس:** حقول إدخال بـ `OutlinedTextField` ذات حواف ناعمة بـ `16.dp` وخلفيات ناصعة البياض تمنح راحة بصرية عالية.
- الأزرار متباعدة ولديها أوزان بصرية واضحة (الزر الأساسي عريض بملصق تعبيري "حفظ وتفعيل ✨").

---

## 5. كسر نمطية التطبيقات التقليدية
- **المساحات البيضاء:** إعطاء هامش لا يقل عن `16.dp` إلى `24.dp` حول كافة المكونات ليتنفس التصميم.
- **البساطة المتناهية:** حذف الإطارات المزدوجة المعقدة والخطوط الفاصلة الحادة، واستبدالها بالفوارق اللونية الطبيعية والظلال الدقيقة الناعمة (Ambient Shadows).
- **التفاعل الصوتي واللمسي البصري:** زر التذكيرات التفاعلي يتحرك ويهتز بنعومة لجذب الانتباه كبديل للأجراس الحمراء الصارخة والمزعجة.

---
**تطبيق هذه المعايير يضمن ارتقاء تطبيق "صلة الرحم" لمراتب التطبيقات الفاخرة التي توفر للمستخدمين واحة بصرية وهدوءاً نفسياً، وتأصيل قيم الترابط بأسلوب يجمع فخامة التصميم واحترافية الكود.**
